/* VERSE 1: Hello guysss, I forgot to manage this file :p */
/* VERSE 2: Lets orginize this file ;) */
/*
Till now.. this feelss like somee unpolished UI.. I wana make it in some theme.. like maybe old radio player. Lets see what I will come up with
till then BYEE!
*/
$main: #FB8500;   /* Amber Glow (Primary accents) */
$sub: #FFB703;    /* Bright Gold (Selection/Focus) */

$bg: #2B2D42;      /* Deep Navy (The Radio Casing) */
$bg-light: #1e213b;/* Antique Cream (The Faceplate) */
$alert: #8E3B1E;   /* Rust Brown (Wood Trim/Alerts) */
$soft: #FB8500;    /* Amber Glow */
$calm: #8E3B1E;    /* Rust Brown */
$gold: #FFB703;    /* Bright Gold */

/*
====================================================================================================================================================
                        GLOBAL STYLES & THEMING
====================================================================================================================================================
*/

Screen {
    layers: transition;
    align: center middle;
    background: $bg;
}

Button:hover {
    text-style: italic;
}

.section-label {
    color: $main;
    margin: 1 0;
    text-style: bold;
}

/*
====================================================================================================================================================
                        WELCOME & SETUP
====================================================================================================================================================
*/

#welcome-text {
    width: auto;
    height: auto;
    color: $gold; 
    text-style: bold;
    content-align: center middle;
}

#skip-hint {
    dock: bottom;
    align: right bottom;
    color: $alert;
    opacity: 0.4;
    padding: 0 1;
}

/* Setup Workspace Layout */


/*
====================================================================================================================================================
                        HOME & MAIN TABS
====================================================================================================================================================
*/
HomeScreen {
    background: $bg;
    color: $gold;
}

#main-app-shell {
    padding: 1 2;
    height: 1fr;
}

#home-tabs {
    height: 1fr;
    margin-bottom: 1;
}

#home-tabs > Tabs {
    background: transparent;
    border: none;
    width: 100%;
    align: center middle;
}

#home-tabs > Tabs > Horizontal {
    width: auto;
}

Tab {
    color: $soft;
    background: transparent;
    text-style: bold;
}

Tabs > .textual-tabs--active {
    color: $gold;
    text-style: underline bold;
}

#home-tabs Underline > .underline--bar {
    color: $gold;
    background: $gold;
}

/* --- CONTENT PANES --- */
TabPane {
    background: $bg-light;
}

/* --- DATA TABLE (The Song List) --- */
DataTable {
    background: $bg-light;
    color: $bg;
    border: tall $alert;
    height: 1fr;
    scrollbar-color: $main;
    scrollbar-background: $bg;
    scrollbar-color-hover: $gold;
    scrollbar-color-active: $gold;
    scrollbar-background-hover: $bg;
    scrollbar-background-active: $bg;
}

DataTable:focus {
    border: tall $gold;
}

DataTable > .datatable--cursor {
    background: $main;
    color: $bg-light;
}

/* --- PLAYLIST BUTTONS (The Creative Part) --- */
.playlist-btn {
    width: 1fr;
    margin: 0 1;
    background: transparent;
    border: tall $alert 30%;
    color: $alert;
    content-align: left middle;
}

.playlist-btn:hover {
    background: $main 10%;
    border: tall $gold;
    color: $gold;
}

/* --- SETTINGS & BUTTONS --- */
.section-label {
    text-style: bold;
    color: $main;
    margin: 1 0;
    padding-left: 1;
}

#btn-regenerate, #btn-add-dir {
    background: $main;
    color: $bg-light;
}

#btn-remove-dir {
    background: $alert;
    color: $bg-light;
}

#btn-resync {
    background: $sub;
    color: $bg-light;
}

/*
====================================================================================================================================================
                        PLAYER SCREEN (FULL FOCUS VIEW)
====================================================================================================================================================
*/

#player-container {
    align: center middle;
    width: 100%;
    height: 100%;
    padding: 1 2;
    background: $bg;
}

/* TOP NAVIGATION */
#top-nav {
    height: 3;
    align: center middle;
    margin-bottom: 1;
}

#playlist-name-title {
    width: 1fr;
    text-align: center;
    text-style: bold;
    color: $gold;
}

#top-nav Button {
    min-width: 4;
    background: transparent;
    border: none;
    color: $gold;
}

/* MIDDLE CONTENT SPLIT */
#middle-content {
    height: 1fr; /* Takes up all space between header and footer */
}

#left-pane {
    width: 50%;
    height: 100%;
    align: left middle;
    padding-left: 10;
}

#right-pane {
    width: 50%;
    height: 100%;
}

/* VISUALISER (ALPHA VERSION) */
#visualiser-container {
    width: 45;
    height: 18;
    border: round $gold;
    background: $bg-light;
    content-align: center middle;
}

#visualiser {
    color: $main;
    text-style: bold;
}

/* ART & INFO (LEFT PANE) */
#art-container {
    width: 40;
    height: 15;
    border: round $alert;
    background: $bg-light;
    content-align: center middle;
}

#big-art {
    content-align: center middle;
    color: $alert;
    text-style: bold;
}

#info-container {
    height: auto;
    width: 40;
    align: left top;
    margin-top: 1;
    padding-left: 1;
}

#song-title {
    text-align: left;
    width: 100%;
    height: 1;
    color: $gold;
    text-style: bold;
}

#song-artist {
    color: $main;
    width: 100%;
    height: 1;
    text-align: left;
    text-style: bold;
    margin-top: 0;
}

/* TIMELINE WRAPPER */
#timeline-wrapper {
    height: auto;
    margin: 1 0;
    align: center middle;
}

#timeline-info {
    height: 3;
    align: center middle;
}

#timeline-slider {
    width: 80%;
    margin: 0 2;
    border: round $gold;
}

#start-time, #end-time {
    width: 7;
    height: 1;
    text-style: bold;
    color: $gold;
    margin: 1;
}

#next-section {
    width: 25%;
    height: 3;
    align: right top;
    padding-top: 1;
}

#auto-play {
    margin-right: 1;
    margin-top: 1;
}

#auto-play-switch {
    margin-top: 0;
}

#button-row {
    height: 5;
    align: center middle;
    margin-top: 1;
}

#auto-play-switch {
    border: none;
    background: transparent;
}

#pause, #resume {
    border: round $gold;
    text-style: bold;
    background: transparent;
    color: $gold;
}

#next, #prev{
    border: round $main;
    text-style: bold;
    background: transparent;
    color: $main;
}

#vol-section {
    width: 25%;
    align: left middle;
}

#vol-slider {
    width: 25;
    border: round $main;
}

#controls-section {
    width: 50%;
    align: center middle;
}

#controls-section Button {
    margin: 0 1;
    min-width: 4;
}

#next-section {
    width: 25%;
    align: right middle;
}

#next-section Label {
    margin-right: 1;
    color: $alert;
}

#go-back {
    width: auto;      
    min-width: 4;
    border: none;
    background: transparent;
    margin-top: 0;
    color: $gold;
}

/*
====================================================================================================================================================
                        MINI PLAYER (PERSISTENT BAR)
====================================================================================================================================================
*/

MiniPlayer {
    dock: bottom;
    height: 7;
    width: 100%;
    background: $bg-light;
    border-top: solid $alert;
}

#mini-info {
    width: 25%;
    height: 100%;
    align: left middle;
}

#mini-title {
    text-style: bold;
    color: $bg;
}

#mini-artist {
    color: $alert;
}

#mini-controls {
    width: auto;
    align: center middle;
    height: 5;
    margin: 0 2;
    background: $bg;
    border: round $main;
}

#mini-controls Button {
    min-width: 4;
    height: 3;
    border: none;
    background: transparent;
    color: $main;
}

/* MINI TIMELINE WRAPPER */
#mini-timeline-wrapper {
    width: 1fr;
    height: 100%;
    align: center middle;

}

#mini-timeline-info {
    height: 3;
    align: center middle;
}

#mini-timeline-slider {
    width: 1fr;
    height: 1;
    margin: 0 1; 
    border: round $gold;
    padding: 0;
}

#mini-start-time, #mini-end-time {
    width: 7;
    height: 1;
    text-style: bold;
    color: $bg;
    margin: 1;
}